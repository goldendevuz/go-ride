services:
  api:
    build: .
    container_name: medic_api
    ports:
      - "4000:4000"
      - "4001:4001"
    env_file:
      - .env
    volumes:
      - .:/usr/src/app
      - ./static:/usr/src/app/static
      - ./media:/usr/src/app/media
    restart: always
    user: root